{
  "name": "niceboys-site",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/generate-sitemap.mjs",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.10.0"
  }
}
